[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "plantdb.client"
version = "0.0.1"
dependencies = [
    "plantdb.commons",
    "requests",
    "tqdm",
]
