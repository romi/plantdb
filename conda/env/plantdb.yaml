{% set data = load_setup_py_data() %}  # loads data from the `setup.py` file
{% set python_requires = data.get('python_requires') %}  # get the 'version' from loaded data

name: plantdb
channels:
  - defaults
  - conda-forge
dependencies:
  - python {{ python_requires }}
  - numpy
  - appdirs
  - colorlog
  - flask
  - flask-cors
  - flask-restful
  - imagecodecs
  - imageio>= 2.16.2
  - pillow
  - scikit-image
  - toml
  - tqdm
  - pip
  - pip:
    - open3d>= 0.9.0.0
